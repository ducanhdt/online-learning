import { makeStyles } from '@material-ui/styles';

export default makeStyles(() => ({
  chip: {
    margin: '8px 8px 8px 8px',
  },
  card: { minHeight: '50vh', maxHeight: '100vh' },
  innerCard: { height: '50%' },
}));
